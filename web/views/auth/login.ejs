<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <title>Auth - Login to view page</title>

  </head>
  <body>
    <div class="container mt-5">
      <div class="row d-flex justify-content-center">
        <div class="col-sm-6 center-block">
          <div class="card">
            <div class="card-header">Login to CheckinGate System</div>
            <div class="card-body">
              <% if(locals.msg) { %>
                <div class="alert alert-danger"><%= msg %></div>
              <% } %>
              <form name="login-form" method="POST" action="/authLogin">
                <div class="form-group">
                  <label for="">Username </label>
                  <input name="txtUsername" class="form-control" id="txtUsername" aria-describedby="emailHelp" placeholder="Enter ussername" />
                </div>
                <div class="form-group">
                  <label for="">Password</label>
                  <input type="password" class="form-control" id="txtPassword" name="txtPassword" placeholder="Password" />
                </div>
                <button type="submit" class="btn btn-primary">Login</button>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
